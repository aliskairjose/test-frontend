import { FilterTutorsPipe } from './filter-tutors.pipe';

describe('FilterTutorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTutorsPipe();
    expect(pipe).toBeTruthy();
  });
});
