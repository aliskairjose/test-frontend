import { FilterBookingPipe } from './filter-booking.pipe';

describe('FilterBookingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBookingPipe();
    expect(pipe).toBeTruthy();
  });
});
